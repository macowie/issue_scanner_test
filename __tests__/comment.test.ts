import {expect, describe, test} from '@jest/globals'
import {createRecommendationCommentIfNeeded} from '../src/comment'

describe('createRecommendationCommentIfNeeded', () => {
  test('skips if already left a comment', async () => {})
})
